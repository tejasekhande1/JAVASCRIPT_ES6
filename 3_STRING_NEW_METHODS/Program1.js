var name = "Tejas";
console.log(`${name}`.startsWith('T'));

console.log(`${name}`.endsWith('T'));

console.log(`${name}`.includes('Tej'));

console.log(`${name} `.repeat(10));

const tName = `${name}${name}`;
console.log(`${tName}`);